# AGENTS.md

## 0. 最高优先级：本项目必须支持中断后恢复

本项目的使用环境网络不稳定，Codex 可能随时因为网络、会话、权限、终端或其他原因中断。

因此，Codex 在本项目中工作时，必须把“任务进程、当前状态、下一步动作、已完成内容、未完成内容、风险点”持续记录到项目文件中，确保任意新会话都可以从仓库文件恢复任务，而不是依赖上一轮聊天记忆。

除非用户明确要求，否则不要依赖聊天上下文作为唯一依据。

---

## 1. 必须维护的状态文件

Codex 必须在项目根目录维护以下文件：

* `.codex/TASK_STATE.md`：当前任务状态，作为恢复任务的第一依据。
* `.codex/WORKLOG.md`：工作日志，只追加，不随意删除历史记录。
* `.codex/NEXT_ACTIONS.md`：下一步待办清单，每项必须足够小，可以独立执行。
* `.codex/DECISIONS.md`：已经确认的产品、技术、架构、命名、兼容性等决策。

如果这些文件不存在，Codex 在开始编码前必须先创建它们。

禁止把密钥、token、密码、私密配置、客户隐私数据写入 `.codex/` 状态文件。

---

## 2. 每次开始工作前的恢复流程

每次 Codex 开始处理任务时，必须先执行以下恢复流程：

1. 读取 `AGENTS.md`。
2. 检查并读取：

   * `.codex/TASK_STATE.md`
   * `.codex/WORKLOG.md`
   * `.codex/NEXT_ACTIONS.md`
   * `.codex/DECISIONS.md`
3. 执行并检查：

   * `pwd`
   * `git branch --show-current`
   * `git status --short`
   * 如有必要，执行 `git diff --stat` 和 `git diff`
4. 根据状态文件和真实代码差异，判断：

   * 已完成什么；
   * 正在做什么；
   * 哪些文件已经被修改；
   * 哪些任务尚未完成；
   * 是否存在未验证、未测试、未提交或半完成状态。
5. 在继续工作前，先更新 `.codex/TASK_STATE.md` 的“恢复检查”部分。
6. 从 `.codex/NEXT_ACTIONS.md` 中第一个未完成的小任务继续执行。

恢复时必须以“仓库当前真实状态”为准，不得只相信上一轮日志描述。

---

## 3. 工作中必须频繁写入检查点

Codex 在以下时机必须更新 `.codex/TASK_STATE.md`、`.codex/WORKLOG.md` 和 `.codex/NEXT_ACTIONS.md`：

* 开始一个新任务前；
* 完成一个最小可验证步骤后；
* 修改超过 3 个文件前；
* 运行测试、构建、迁移、格式化等重要命令前后；
* 遇到报错、阻塞、权限问题、依赖问题时；
* 准备切换方向、重构、删除代码、改数据库结构前；
* 每次阶段性完成后；
* 会话即将结束或用户要求总结时。

每次更新检查点时，必须写清楚：

* 当前任务名称；
* 当前状态：`not_started` / `in_progress` / `blocked` / `needs_review` / `done`；
* 已完成事项；
* 正在处理的文件；
* 下一步最小动作；
* 已知风险；
* 需要验证的命令；
* 如果中断，恢复后应该从哪里继续。

---

## 4. `.codex/TASK_STATE.md` 标准格式

Codex 必须保持 `.codex/TASK_STATE.md` 使用以下结构：

# Task State

## Current Task

* Task ID:
* Task Name:
* User Request:
* Status:
* Started At:
* Last Updated:

## Current Goal

简要说明当前阶段目标。

## Completed

* [ ] 已完成事项 1
* [ ] 已完成事项 2

## In Progress

* 当前正在处理的事项：
* 当前涉及文件：

## Next Actions

* [ ] 下一步 1
* [ ] 下一步 2
* [ ] 下一步 3

## Modified Files

* `path/to/file`：修改目的

## Verification

* 已运行命令：
* 结果：
* 尚未运行但需要运行的命令：

## Risks / Notes

* 风险点：
* 注意事项：

## Recovery Instructions

如果任务中断，下一次 Codex 必须：

1. 读取本文件；
2. 执行 `git status --short`；
3. 检查 Modified Files 中的文件；
4. 从 Next Actions 第一项未完成任务继续；
5. 继续前先补充一条恢复日志。

---

## 5. `.codex/WORKLOG.md` 规则

`.codex/WORKLOG.md` 是追加式日志。Codex 不得随意删除旧日志。

每条日志格式：

## YYYY-MM-DD HH:mm - 简短标题

* 做了什么：
* 修改了哪些文件：
* 为什么这样做：
* 验证结果：
* 下一步：

如果发生中断恢复，必须追加：

## YYYY-MM-DD HH:mm - Recovery

* 检查到的仓库状态：
* 已确认完成：
* 未完成：
* 决定从哪里继续：

---

## 6. `.codex/NEXT_ACTIONS.md` 规则

`.codex/NEXT_ACTIONS.md` 必须只保留当前最重要的下一步动作。

要求：

* 每项任务必须足够小。
* 每项任务最好能在 5 到 20 分钟内完成。
* 不要写模糊任务，例如“优化代码”。
* 应写成可执行任务，例如“检查 `src/pages/settings.tsx` 中体重输入是否支持 1 位小数”。

标准格式：

# Next Actions

* [ ] 1. 具体动作
* [ ] 2. 具体动作
* [ ] 3. 具体动作

## Done This Session

* [x] 已完成动作

---

## 7. `.codex/DECISIONS.md` 规则

重要决策必须记录到 `.codex/DECISIONS.md`。

包括但不限于：

* 技术选型；
* 字段命名；
* 数据结构；
* 兼容性策略；
* 是否保留旧逻辑；
* 是否新增依赖；
* 用户明确确认过的产品规则；
* 不做某件事的原因。

格式：

## YYYY-MM-DD - 决策标题

* 决策：
* 原因：
* 影响范围：
* 后续注意：

---

## 7.1 方案成本与付费服务评估规则

Codex 在选择、调整或实施任何开发、部署、数据库、存储、网络、消息、监控、第三方 API 等方案前，必须把成本作为关键约束进行评估。

尤其是涉及云服务时，不得只评估技术可行性，也必须提前识别可能产生费用的资源，包括但不限于：

* 私有网络 / VPC / 内网互通；
* NAT 网关、固定公网 IP、负载均衡、带宽、流量包；
* 云数据库、备份、只读实例、连接器；
* 对象存储、CDN、图片处理、日志存储；
* 云函数、云托管、容器实例、定时任务；
* 短信、邮件、地图、AI、第三方 SaaS 或 API；
* 域名、证书、监控告警和安全产品。

在进入部署、迁移或采购前，Codex 必须：

1. 列出当前方案依赖的新增付费项或潜在付费项；
2. 判断这些费用是否与预期访问量、试运行阶段和用户预算相匹配；
3. 至少给出一个更低成本或更简单的替代方案，除非不存在合理替代；
4. 在 `.codex/DECISIONS.md` 或 `.codex/PLANS.md` 记录成本假设、已知费用风险和用户最终选择；
5. 如果费用规则可能变化，必须让用户以当前云厂商控制台或官方价格页为准确认，不得假设历史价格仍然有效；
6. 若部署中途发现额外付费服务，必须先暂停并向用户说明，再继续操作。

禁止把项目推到部署或迁移中后段，才首次告知用户必须开通额外收费服务。若成本不确定，应在方案阶段明确标记为风险。

---

## 8. 编码规则

Codex 必须遵守以下规则：

1. 先理解任务，再修改代码。
2. 优先小步修改，避免一次性大范围重构。
3. 不得无说明删除已有功能。
4. 不得擅自改动与当前任务无关的文件。
5. 不得擅自引入新依赖，除非用户明确同意。
6. 不得把临时测试、调试代码遗留在生产代码中。
7. 修改前先看相关上下文，避免只改局部导致整体逻辑断裂。
8. 如果发现用户需求和现有代码冲突，应记录到 `.codex/TASK_STATE.md`，并采用最小安全改动。
9. 能通过现有结构解决的问题，不要重建项目结构。
10. 每次阶段完成后必须更新状态文件。

---

## 9. Git 与差异检查规则

每次开始工作前必须执行：

```bash
git status --short
```

每次阶段完成后建议执行：

```bash
git diff --stat
```

如涉及关键逻辑，必须检查具体 diff。

除非用户明确要求，否则不要自动提交 commit。

如果用户允许提交，则每完成一个可验证阶段可以提交一次，提交信息必须清晰，例如：

```bash
git add .
git commit -m "feat: add resumable task state tracking"
```

---

## 10. 验证规则

Codex 必须优先使用项目已有的验证方式。

常见命令包括但不限于：

```bash
npm run lint
npm run typecheck
npm run build
npm test
```

如果项目没有这些命令，Codex 必须先检查 `package.json`、README 或项目文档，找到真实可用命令。

如果无法运行测试或构建，必须在 `.codex/TASK_STATE.md` 中写明原因，不能假装已验证。

---

## 11. 中断恢复时的行为规则

当 Codex 发现上次任务可能中断时，必须：

1. 不要重新从零开始。
2. 不要重复已经完成的修改。
3. 不要覆盖未确认的用户改动。
4. 先读取 `.codex/` 状态文件。
5. 再检查 `git status` 和 `git diff`。
6. 对照代码实际状态，判断日志是否准确。
7. 更新一条 Recovery 日志。
8. 从第一个未完成的最小任务继续。

如果发现状态文件和代码实际情况不一致，以代码实际情况为准，并修正状态文件。

---

## 12. 对用户的汇报规则

Codex 完成阶段任务后，必须向用户简要汇报：

* 已完成什么；
* 修改了哪些文件；
* 如何验证；
* 是否存在未完成事项；
* 如果中断，下次应从哪里继续。

汇报要简洁，不要堆砌无关技术细节。

---

## 13. 复杂任务规则

如果任务涉及以下情况，Codex 必须先创建或更新 `.codex/TASK_STATE.md` 和 `.codex/NEXT_ACTIONS.md`，再开始编码：

* 多页面修改；
* 数据结构调整；
* 数据库 schema 修改；
* 登录、权限、支付、订单、安全相关逻辑；
* 大范围重构；
* 超过 5 个文件的修改；
* 用户说“继续上次任务”；
* 用户说“按照之前方案继续”；
* 网络不稳定、任务可能中断的长任务。

复杂任务必须拆成多个可恢复的小步骤。

---

## 14. 用户常用恢复指令识别

当用户输入以下类似内容时，Codex 必须自动进入恢复模式：

* “继续”
* “继续上次任务”
* “接着做”
* “刚才中断了”
* “网络断了，继续”
* “检查当前进度”
* “恢复任务”
* “根据状态文件继续”
* “不要重做，接着上次的做”

恢复模式下，第一步永远是读取 `.codex/` 状态文件和检查 git 状态。

## 15. 长任务执行计划

当任务预计涉及多个阶段、多个文件或超过一次会话时，Codex 必须创建 `.codex/PLANS.md`。

`.codex/PLANS.md` 必须包含：

- 背景
- 目标
- 非目标
- 当前代码理解
- 分阶段计划
- 每阶段验收标准
- 当前进度
- 已完成
- 未完成
- 风险
- 恢复说明

每完成一个阶段，必须更新 `.codex/PLANS.md`。
