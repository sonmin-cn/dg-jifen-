# 队长积分管理系统

内部队长积分管理系统 MVP。当前阶段完成 Next.js、TypeScript、Tailwind CSS、shadcn/ui 约定、Prisma 和 SQLite 本地开发数据库的基础框架。

## 开发命令

```bash
npm install
npm run prisma:generate
npm run prisma:push
npm run dev
```

开发服务器默认运行在 `http://localhost:3000`。
